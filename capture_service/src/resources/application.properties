spring.application.name=Scanner

spring.jpa.hibernate.ddl-auto=validate
spring.datasource.url=${URLDB}
spring.datasource.username=${USERNAMEDB}
spring.datasource.password=${PASSWORDDB}
server.port=8081
app.security.secret-key=${SECRET_KEY}
spring.jackson.deserialization.fail-on-unknown-properties=true

plotter.service.url=http://plotter:6767/generate-plot
app.internal.secret=${PLOT_SECRET_KEY}